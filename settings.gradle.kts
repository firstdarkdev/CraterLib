pluginManagement {
    repositories {
        mavenLocal()
        maven("https://mcentral.firstdark.dev/releases")
        gradlePluginPortal()
        maven("https://maven.firstdark.dev/releases")
        maven("https://maven.firstdark.dev/snapshots")
    }
}

rootProject.name = "CraterLib-lts"